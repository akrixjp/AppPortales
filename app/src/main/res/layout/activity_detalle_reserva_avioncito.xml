<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="pe.edu.sise.appportales.DetalleReservaAvioncitoActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <Spinner
            android:id="@+id/sp_nivel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:entries="@array/array_nivel"
            android:prompt="@string/promt_sp_nivel"
            android:spinnerMode="dialog">

        </Spinner>
        <Spinner
            android:id="@+id/sp_zona"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:entries="@array/array_zona"
            android:prompt="@string/promt_sp_nivel"
            android:spinnerMode="dialog">

        </Spinner>
        <GridView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:columnWidth="70dp"
            android:numColumns="auto_fit"
            android:gravity="center"
            android:id="@+id/am_gv_gridview"
            >

        </GridView>
    </LinearLayout>

</LinearLayout>
